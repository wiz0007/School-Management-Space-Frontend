# Visitor Hero Assets

Place the public home page background video here as:

```text
school-hero.mp4
```

The Angular home page already includes a video source for `/assets/visitor/school-hero.mp4`. If the file is missing, the CSS fallback still renders the hero with a school-tech visual treatment.